apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: plataform
rules:
- apiGroups: ["", "apps"]
  resources: ["pods", "deployments"]
  verbs: ["list", "create", "get", "delete"]
